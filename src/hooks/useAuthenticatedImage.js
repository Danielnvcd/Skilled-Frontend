// Conservado por retrocompatibilidad con consumidores existentes (Quejas.jsx).
// La implementación real vive en useAuthenticatedBlob — el hook sirve para
// cualquier tipo de archivo, no solo imágenes.
export { default } from './useAuthenticatedBlob'
